import React from "react";
import style from "./End.module.css";

const End = () => {
  return (
    <section className="w-screen h-screen flex justify-center items-end py-12">
      <div className="container mx-auto">
        <p className="text-3xl text-center w-10/12 mx-auto">
          In the end, the Ring plummeted into the fiery depths of Mount Doom,
          its birthplace. In that instant, the source of darkness was unmade,
          Sauron's power shattered, and hope rekindled across the land.
        </p>
      </div>
    </section>
  );
};

export default End;
